#include <iostream>
#include <fstream>
#include <vector>
#include <cstdint>
#include <cassert>

uint32_t pack_color(const uint_t r, const uint8_t g, const uint8_t b, const uint8_t a=255) { 
    return (a<<2on
}
